[package]
name = "cageforge-bwrap"
version.workspace = true
edition.workspace = true
license = "Apache-2.0 AND LGPL-2.0-or-later"
description = "Builds the upstream Bubblewrap executable for Cageforge Linux packages"
repository.workspace = true
readme = "README.md"
build = "build.rs"

[[bin]]
name = "cageforge-bwrap"
path = "src/main.rs"

[build-dependencies]
cc = { workspace = true }
pkg-config = { workspace = true }

[dependencies]
sha2 = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
