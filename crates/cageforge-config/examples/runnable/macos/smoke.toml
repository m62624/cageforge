# Runnable macOS smoke profile. Run from the repository root with:
# cargo run --locked -p cageforge-cli --no-default-features --features macos -- \
#   run --config crates/cageforge-config/examples/runnable/macos/smoke.toml
default_profile = "smoke"

[profiles.smoke]
workspace_roots = { "." = true }

[profiles.smoke.filesystem]
mode = "restricted"
rules = [
  { target = "minimal", access = "read" },
  { target = "workspace-root", access = "write" },
]

[profiles.smoke.network]
mode = "disabled"

[profiles.smoke.command]
program = "/bin/echo"
args = ["cageforge-macos-smoke"]
